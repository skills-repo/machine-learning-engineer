# 模型评估框架 Playbook

> 选错指标比没指标更危险。本 playbook 给出按任务选指标、建验证、防泄漏的评估方法。

## 决策树：用什么指标

```
明确任务类型
  ├─ 分类 → 准确率/精确率/召回率/F1/AUC（看类别平衡与代价）
  ├─ 回归 → MAE/RMSE/R²（看量纲与离群）
  ├─ 排序 → NDCG/MRR/AUC
  └─ 生成 → 人工评审 + 自动代理指标（不盲信单一分数）
关注点：
  ├─ 类别不平衡 → 看召回率/PR 而非准确率
  ├─ 误判代价不对称 → 看混淆矩阵与代价敏感指标
  └─ 需校准概率 → 看 Brier / reliability curve
```

## 验证策略

- **留出 / 交叉验证**：数据量决定；小数据用 CV。
- **分层切分**：分类任务保持类别比例。
- **时间序列**：按时间切，禁随机洗牌（未来泄露过去）。

## 基线先行

- 永远先建简单基线（均值/规则/逻辑回归），再上复杂模型。
- 复杂模型的增益必须显著且可解释，否则不值复杂度。

## 防泄漏

- 切分前不做用到标签的预处理（归一化统计量、特征选择）。
- 训练/验证/测试严格分离；测试集仅最终用一次。
- 检查是否有重复样本横跨切分（用 `scripts/dataset_split_check.py`）。

## 输出

对一次评估给出：指标是否合适、验证是否严谨、有无泄漏迹象、基线对比结论。不替用户拍板上线。

## 边界

- 不给"模型够不够好"的绝对判断，给相对基线与目标的差距。
- 业务指标（收入/留存）高于离线指标，需标注鸿沟。

## 相关子技能与层次边界（L2→L3）

本 playbook 提供「按任务选指标、防泄漏验证」的评估方法（L2 决策层），落地到以下子技能：

- `skills/machine-learning/SKILL.md` — 模型选择与评估的核心方法
- `skills/deep-learning-pytorch/SKILL.md` — 深度学习模型的验证与调优
- `skills/computer-vision-opencv/SKILL.md` — 视觉任务的指标与验证策略
- `skills/pytorch-patterns/SKILL.md` — 可复现的评估实验设计

边界：本篇给指标 / 验证 / 泄漏的相对判断，不替 `skills/` 下的子技能拍板上线。
